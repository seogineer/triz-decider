---
description: Run a TRIZ contradiction analysis (technical or physical) on a problem
argument-hint: <problem description> [--save]
---
Load the `triz-solver:triz-analysis` skill with the Skill tool now (use this exact namespaced name), then follow its instructions exactly to analyze the problem below. Do not answer from memory: matrix cells, separation types, principle numbers and principle definitions must come from the skill's `lookup.py` output.

If the arguments contain `--save`, leave it out of the problem text and save the finished analysis as a Markdown report as the skill describes.

Problem: $ARGUMENTS
