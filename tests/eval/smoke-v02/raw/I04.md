This description reads more like an already-built solution (a screw combining a main thread + a locking sub-thread) than a stated trade-off, so per the TRIZ workflow I should first find out what conflict this design is solving rather than guess.

Could you tell me: if you only used a single (ordinary) thread, what goes wrong across those varied materials — and what happens (what gets worse, or what becomes harder) once you add the sub-thread to fix it?