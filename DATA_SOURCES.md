# DATA_SOURCES

데이터 출처·검증 상태 기록. 설계: `docs/DESIGN.md` 9장.
**현재 상태: 모든 데이터가 출처 미확인(UNVERIFIED) 레거시 자료의 변환본이다.** 공개 전 아래 TODO를 해소해야 한다.

## 1. 레거시 데이터 (변환 원본)
- 원본: 커밋 `ae96eee`에 포함된 `skills/triz-analysis/data/*.json` 3종. 변환 전 상태는 변환 커밋의 부모에서 `git show <parent>:skills/triz-analysis/data/<file>`로 복원 가능
- 원본의 출처·작성 방법은 기록이 없다. 아래 항목은 전부 "출처 미확인"으로 취급한다.

## 2. contradiction-matrix.json — 행렬 셀 값 (최우선)

변환 결과: 1138개 셀 보유, `missing_rows: [16, 20]`.

### 2.1 확인된 결손 (사실)
| 항목 | 내용 |
| --- | --- |
| 행 16 (개선=작동 지속 시간(정지)) 전체 없음 | 38셀 미보유 |
| 행 20 (개선=에너지 사용(정지)) 전체 없음 | 38셀 미보유 |
| 빈 배열(`[]`) 셀 268개 | 원본에서 "원리 없음"인지 "미입력"인지 구분 불가. 변환 시 키 생략(5.2 규칙) |

`missing_rows`는 5장 스키마에 없는 확장 필드다. 없으면 `lookup.py`가 16·20행을 정상 빈 셀로 오인해 `empty_pairs`로 반환한다. 누락 행은 **채우지 않았다**(CLAUDE.md: 추측·보간 금지).

### 2.2 의심 패턴 (교차 검증 우선 대상)
동일한 원리 배열이 서로 무관해 보이는 4곳 이상의 셀에 반복된다(전체 40종). 고전 행렬에도 반복은 있으나 다음은 사람이 원문과 대조해야 한다.
- `[6,29,4,38]`: 1-17, 1-36, 2-17, 5-36, 25-5
- `[11,3,10,32]`: 1-27, 2-27, 5-27, 13-14, 27-7, 27-8, 27-36
- `[28,26,18,35]`: 1-37, 2-37, 13-38, 19-37, 37-1, 37-19, 38-13
- `[22,10,2]`: 8-30, 11-31, 19-30, 24-30, 25-30, 30-25, 30-26
- 재현 스크립트: 셀 값별로 키를 묶어 길이 ≥4인 그룹 출력 (전체 목록은 검증 단계에서 생성)

### 2.3 설계서 예시와의 불일치
- DESIGN.md 6.2 예시는 `9-2 → [2,28,13,38]`이나 데이터의 셀 9-2는 비어 있다(`9-14`는 예시와 일치). 예시가 "형식 예시일 뿐"이라고 명시돼 있어 데이터를 바꾸지 않았고, 교차 검증 시 이 셀을 우선 확인한다.

### 2.4 TODO
- [ ] 교차 검증용 공개 자료 2종 이상 선정 (DESIGN.md 11.2 미결) — 목록·라이선스·접근일 기록
- [ ] 1138개 보유 셀 전수 대조 (자료 간 상이 시 채택 기준 기록)
- [ ] 행 16, 20 (76셀) 확인된 값만 입력
- [ ] 빈 셀 268개가 진짜 빈 칸인지 확인
- [ ] 완료 후 `missing_rows`를 `[]`로 하고 `source` 갱신

## 3. inventive-principles.json
- [ ] `sub_principles`는 원본의 `description` 문장을 `.`/`;` 기준으로 분할한 것. 원문 분할이 하위 원리 경계와 일치하는지 확인 필요
- [ ] **영문 텍스트(`en`)**: 고전 문헌의 표준 영어 표현과 매우 유사한 문장이 있다(예: 원리 16). 서적 문장 복제 금지 원칙에 따라 직접 재작성 필요
- [ ] 한국어 텍스트·예시: 원본이 번역본을 복제했는지 확인, 필요 시 재작성
- [ ] 원리 명칭 40개: 표준 명칭과 대조

## 4. parameters.json
- 39개 ID·영문/한글 명칭은 레거시 행렬 파일의 `parameters` 배열에서 가져왔다.
- **명칭 수정 2건 (검증 대기)**: 레거시의 #30 `Object-generated harmful effects`, #31 `Harmful side effects`는 의미가 겹쳐 널리 쓰이는 표준 목록과 다르다. 표준 목록 기준으로 #30 `Object-affected harmful factors`(물체가 받는 유해 요인), #31 `Object-generated harmful factors`(물체가 발생시키는 유해 요인)로 바꿨다. 행렬은 번호 위치로 조회하므로 열 의미가 어긋나면 결과가 틀어진다. 이 수정은 작성자 지식에 근거한 것이며 공개 자료로 확인해야 한다.
- 정의(ko/en)·키워드 39개는 이 프로젝트에서 직접 작성했다(문헌 문장 미사용). 저작권 검토 대상은 아니나 내용 정확성은 리뷰가 필요하다.
- [ ] 나머지 명칭 37개를 표준 목록과 대조
- [ ] 정의·키워드 리뷰, 평가 케이스(10.1) 결과에 따라 키워드 보강
- `references/parameters-39.md`는 `parameters.json`에서 생성한 파일이며 동기화는 `tests/test_data_integrity.py`가 검사한다.

## 5. separation-principles.json (v0.2 범위)
- ID를 `SP1~4` → `time/space/system/condition`으로 변환
- [ ] `question.ko` 4개 `TODO` (원본에 질문 필드 없음)
- [ ] 연계 발명 원리 목록 출처 미확인 (DESIGN.md 5.4: 채택 출처를 `source`와 README에 기록)
- [ ] 예시 문장의 독창성 확인
